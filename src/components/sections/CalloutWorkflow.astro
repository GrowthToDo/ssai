---
const title = "Auto-Recommended Replacements";

const steps = [
  {
    step: "1",
    description: "Call-out or leave approved → System automatically searches for candidates",
  },
  {
    step: "2",
    description: "Checks availability, qualifications, hours worked, rest requirements, 60h limit",
  },
  {
    step: "3",
    description: "Follows escalation ladder: Float Pool → PRN → Overtime → Agency",
  },
  {
    step: "4",
    description: "Presents top 3 candidates with explanations → You approve one click",
  },
];

const candidateReasons = [
  "Float pool staff - designed for coverage",
  "Cross-trained for ICU",
  "PRN staff - marked available for this date",
  "High reliability rating (5/5)",
  "No overtime (within 40 hours)",
  "Low flex hours YTD (fair distribution)",
];

const output = "Top 3 ranked candidates + Reasons for each + One-click approval → Assignment auto-created";
---

<section class="bg-linear-to-b from-slate-50 via-white to-white">
  <div class="mx-auto max-w-[85rem] px-4 py-16 sm:px-6 lg:px-8 lg:py-24">
    <h2 class="text-center text-balance text-3xl font-semibold tracking-tight text-slate-900 sm:text-4xl">
      {title}
    </h2>
    <p class="mt-4 text-center text-lg text-slate-600">When someone calls out or leave is approved, you don't hunt for replacements—the system does.</p>

    <div class="mt-12 grid gap-8 lg:grid-cols-2">
      <div>
        <h3 class="text-lg font-semibold text-slate-900 mb-6">How it works</h3>
        <ol class="space-y-5">
          {steps.map((item) => (
            <li class="flex items-start gap-4">
              <span class="flex size-9 shrink-0 items-center justify-center rounded-full bg-teal-700 font-bold text-white text-sm">
                {item.step}
              </span>
              <p class="mt-1 text-slate-700">{item.description}</p>
            </li>
          ))}
        </ol>
      </div>

      <div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
        <h3 class="text-lg font-semibold text-slate-900 mb-4">Each candidate includes reasons</h3>
        <p class="text-sm text-slate-600 mb-4">So you know exactly why they're recommended:</p>
        <ul class="space-y-2">
          {candidateReasons.map((reason) => (
            <li class="flex items-center gap-2 text-sm">
              <span class="text-teal-600">✓</span>
              <span class="text-slate-700 italic">"{reason}"</span>
            </li>
          ))}
        </ul>
      </div>
    </div>

    <div class="mt-10 mx-auto max-w-2xl rounded-2xl bg-teal-50 p-6">
      <p class="text-sm font-semibold text-slate-900">Result:</p>
      <p class="mt-2 text-slate-700">{output}</p>
    </div>
  </div>
</section>
